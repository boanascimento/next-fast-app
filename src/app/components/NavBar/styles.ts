'use client';

import { EColor } from '@/utils/styles';
import styled from 'styled-components';

export const NavBarContainer = styled.nav`
  min-width: 230px;
  width: 18%;
`;

export const MenuTitle = styled.h1`
  color: ${EColor.grayScale_100};
  text-align: center;
`;
